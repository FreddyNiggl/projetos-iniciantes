// Crie uma função que recebe dois parâmetros, 
// base e expoente, e retorne a base elevada ao expoente.

const expo = function(x, y) {
    console.log(`${x} elevado a ${y} = ${x ** y}`)
}

expo(2,4)

a = 2
b = 4
expoente= Math.pow(a, b)
console.log(expoente)
 